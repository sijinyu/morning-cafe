// The map UI is rendered by PersistentMapPage in the layout (always mounted).
// This page intentionally returns null so the persistent map shows through.
export default function MapPage() {
  return null;
}
